document.addEventListener('DOMContentLoaded', function() {
  // Example: simulate fetching live 40°C+ days
  const daysEl = document.getElementById('heat-days');
  if (daysEl) {
    // Simulate API call or calculation
    daysEl.innerText = '87'; // Replace with dynamic logic or API
  }
});
